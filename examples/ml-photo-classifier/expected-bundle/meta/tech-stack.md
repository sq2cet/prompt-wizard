# Tech stack

## Form factor

- **Primary:** ml
- **Secondary:** cli

**Why this choice:** The bulk of the value is the classifier itself; the CLI is just the harness.

## Mandated stack (per `docs/15-constraints.md`)

> The user has mandated:
> 
> Python with a pre-trained vision model from Hugging Face.

The catalog recommendation engine is **suppressed** for this build — use the mandated stack as written. Do not propose alternatives unless the human asks.

**Forbidden tech** (also from constraints): Cloud APIs (privacy).

The catalog suggestions below are kept for reference only. **Treat them as inadmissible** unless the human revises the constraint.

## Catalog reference (do not use unless the constraint is revised)

_(No catalog entry for primary form factor `ml`. The build agent should propose a stack based on the constraints in `docs/15-constraints.md` and confirm with the human.)_