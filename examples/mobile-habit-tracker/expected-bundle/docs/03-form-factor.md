# Phase 3: Form factor

> Web, mobile, desktop, CLI, etc. Multi-select with one chosen as primary.

**Mode:** detailed

## What kind of application is this?
> Pick all that apply. A web app with a REST API is two checkboxes — both will be reflected in the prompt.

- Mobile app

## Of those, which is the primary one?
> The dependency graph and stack recommendation key off this. Secondary factors become constraints.

Mobile app (`mobile`)

**Why this choice (user note):** The app needs to live on the home screen and prompt the user during their day; only a native mobile shape does that.

## How will users get the app?

From an app store (mobile / desktop) (`app_store`)
